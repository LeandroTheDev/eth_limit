# ETH LIMIT
Limit the server port internet usage with shellscript (Linux only)

Unfurtunally requires root (sudo)

### Usage
- ./limit.sh 80 500kbps
- > Limit the ports 80 (http) to only 500kbps

### Disable
- ./removelimit.sh